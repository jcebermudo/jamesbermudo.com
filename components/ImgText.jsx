const ImgText = ({ text }) => {
  return (
    <div className="flex flex-row items-center justify-center">
      <p className="italic text-center opacity-50 my-0">{text}</p>
    </div>
  );
};

export default ImgText;
